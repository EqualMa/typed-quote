[package]
name = "test-proc-macro"
version = "0.1.0"
edition = "2024"
publish = false

[lib]
proc-macro = true

[dependencies]
typed-quote = { version = "0.1.0", path = "../..", features = ["proc-macro"] }
